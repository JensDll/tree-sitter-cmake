=======
if [if]
=======

if()
endif()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (endif_command
      (endif))))

===============
if, elseif [if]
===============

if()
elseif()
endif()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (elseif_command
      (elseif))
    (body)
    (endif_command
      (endif))))

=====================
if, elseif, else [if]
=====================

if()
elseif()
else()
endif()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (elseif_command
      (elseif))
    (body)
    (else_command
      (else))
    (body)
    (endif_command
      (endif))))

=======================
if, elseif, elseif [if]
=======================

if()
elseif()
elseif()
endif()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (elseif_command
      (elseif))
    (body)
    (elseif_command
      (elseif))
    (body)
    (endif_command
      (endif))))

=============================
if, elseif, elseif, else [if]
=============================

if()
elseif()
elseif()
else()
endif()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (elseif_command
      (elseif))
    (body)
    (elseif_command
      (elseif))
    (body)
    (else_command
      (else))
    (body)
    (endif_command
      (endif))))

============
if body [if]
============

if(TARGET t)
  message(STATUS abc)
  # foo
endif()

---

(source_file
  (if_statement
    (if_command
      (if)
      (argument_list
        (unquoted_argument
          (text))
        (unquoted_argument
          (text))))
    (body
      (command
        (identifier)
        (argument_list
          (unquoted_argument
            (text))
          (unquoted_argument
            (text))))
      (comment))
    (endif_command
      (endif))))

================
elseif body [if]
================

if()
elseif(${x})
  #[[ body ]]
endif()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (elseif_command
      (elseif)
      (argument_list
        (unquoted_argument
          (normal_variable
            (variable_content
              (text))))))
    (body
      (comment
        (bracket_arguemnt
          (bracket_open)
          (bracket_content)
          (bracket_close))))
    (endif_command
      (endif))))

==============
else body [if]
==============

if()
else(foo)
  bar()
endif()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (else_command
      (else)
      (argument_list
        (unquoted_argument
          (text))))
    (body
      (command
        (identifier)))
    (endif_command
      (endif))))

==================================
whitespace after command name [if]
==================================

if  ()
elseif ()
else ()
endif ()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (elseif_command
      (elseif))
    (body)
    (else_command
      (else))
    (body)
    (endif_command
      (endif))))

==============
arguments [if]
==============

if((condition) AND (condition OR (condition)))
elseif(TARGET foo)
else(DEFINED bar)
endif(
# done
)

---

(source_file
  (if_statement
    (if_command
      (if)
      (argument_list
        (argument_list
          (unquoted_argument
            (text)))
        (unquoted_argument
          (text))
        (argument_list
          (unquoted_argument
            (text))
          (unquoted_argument
            (text))
          (argument_list
            (unquoted_argument
              (text))))))
    (body)
    (elseif_command
      (elseif)
      (argument_list
        (unquoted_argument
          (text))
        (unquoted_argument
          (text))))
    (body)
    (else_command
      (else)
      (argument_list
        (unquoted_argument
          (text))
        (unquoted_argument
          (text))))
    (body)
    (endif_command
      (endif)
      (argument_list
        (comment)))))

=====================
case insensitive [if]
=====================

iF()
ELSEif()
eLsE()
ENDIF()

---

(source_file
  (if_statement
    (if_command
      (if))
    (body)
    (elseif_command
      (elseif))
    (body)
    (else_command
      (else))
    (body)
    (endif_command
      (endif))))
